import 'package:flutter/material.dart';

class RadioPreferences extends StatelessWidget {
  const RadioPreferences({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(

      ),
    );
  }
}
